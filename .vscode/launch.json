{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch API",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/api/target/debug/api",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}